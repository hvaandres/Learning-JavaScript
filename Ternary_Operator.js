function ageVerification(age) {
    // if (age > 25) {
    // console.log('can rent a car');
    // } else {
    //    console.log('is not old enough yet');

    // let answer = age > 25 ? 'can rent a car' : "can't ren a car";
    // console.log(answer);

    age > 25 ? console.log('can rent a car') : console.log("cant't rent a car");
}

ageVerification(5)
