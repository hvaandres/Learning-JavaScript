// Different way to create arrays
// To call or see the array, you will need to type generatedArray

var generatedArray = new Array(3);

var generatedArray = new Array('Andres', 'Adrian', 'Humberto');

//Literal Array

var literalArray = [1, 2, 3];

// To see how many arrays are stored in the line, you will need to type literalArray.lenght;

var mixedArray = ['Hi', 1, ['a', 'b', 'c'], { name: Andres }, function greeting() { console.log('hey there'); }]

// How to name one of the items from your array

var playerName = literalArray[1];
// Results: Adrian